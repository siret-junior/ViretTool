<Window x:Class="ViretTool.PresentationLayer.Windows.Views.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:cal="http://www.caliburnproject.org"
			 xmlns:properties="clr-namespace:ViretTool.Resources.Properties;assembly=ViretTool.Resources"
        xmlns:converters="clr-namespace:ViretTool.PresentationLayer.Converters"
			 xmlns:common="clr-namespace:ViretTool.PresentationLayer.Controls.Common"
			 xmlns:viewModels="clr-namespace:ViretTool.PresentationLayer.Windows.ViewModels"
			 mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
			 Title="{x:Static properties:Resources.AppTitle}"
             WindowStyle="None" ResizeMode="NoResize"  
             WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
                    <converters:InvertedBooleanConverter x:Key="InvertedBooleanConverter" />
                </ResourceDictionary>
                <ResourceDictionary>
                    <DataTemplate x:Key="GroupHeaderTemplate">
                        <TextBlock Text="{Binding}" FontSize="20" FontWeight="Bold" Foreground="Red"/>
                    </DataTemplate>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>


    <common:BusyIndicator IsBusy="{Binding IsBusy}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- left panel (models) -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Menu Grid.Row="0" Width="Auto" Background="White">
                        <MenuItem Header="{x:Static properties:Resources.FileHeader}">
                            <MenuItem Header="{x:Static properties:Resources.OpenDatabaseHeader}" cal:Message.Attach="[Event Click] = [Action OpenDatabase]"/>
                        </MenuItem>
                    </Menu>

                    <GroupBox Grid.Row="1" Margin="6" Header="{x:Static properties:Resources.TemporalQuery1Header}" HeaderTemplate="{StaticResource GroupHeaderTemplate}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <CheckBox Content="{x:Static properties:Resources.PrimaryQueryLabel}" IsChecked="{Binding IsFirstQueryPrimary}" />
                            <ContentControl Grid.Row="1" x:Name="Query1" />
                        </Grid>
                    </GroupBox>
                    <GroupBox Grid.Row="2" Margin="6" Header="{x:Static properties:Resources.TemporalQuery2Header}" HeaderTemplate="{StaticResource GroupHeaderTemplate}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <CheckBox Content="{x:Static properties:Resources.PrimaryQueryLabel}" IsChecked="{Binding IsFirstQueryPrimary, Converter={StaticResource InvertedBooleanConverter}}" />
                            <ContentControl Grid.Row="1" x:Name="Query2" />
                        </Grid>
                    </GroupBox>

                </Grid>
            </ScrollViewer>

            <GridSplitter  Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

            <!-- result displays -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="5" />
                        <ColumnDefinition Width="110" />
                    </Grid.ColumnDefinitions>

                    <ContentControl Grid.Column="0" x:Name="QueryResults" />
                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
                    <ContentControl Grid.Column="2" x:Name="DetailView" />
                </Grid>
            </Grid>
        </Grid>
    </common:BusyIndicator>
</Window>
